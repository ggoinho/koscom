<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#00000000"
    android:id="@+id/container">

    <LinearLayout
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:background="@drawable/shape_rect_fill38"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:padding="25dp"
        android:orientation="vertical"
        android:clickable="true">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="15dp"
            android:textSize="17dp"
            android:textStyle="bold"
            android:textColor="#333333"
            android:letterSpacing="-0.03"
            android:text="Push 수신동의"/>

        <LinearLayout
            android:id="@+id/confirmZone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">
            <androidx.core.widget.ContentLoadingProgressBar
                android:id="@+id/progress_bar_login"
                style="?android:attr/progressBarStyleLarge"
                android:layout_width="50dp"
                android:layout_height="50dp"/>
            <TextView
                android:id="@+id/message"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="2"
                android:layout_marginLeft="15dp"
                android:textSize="15dp"
                android:textColor="#a0a0a0"
                android:letterSpacing="-0.03"
                android:lineSpacingExtra="5dp"
                android:text="Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의 Push 수신동의"/>
        </LinearLayout>


    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>